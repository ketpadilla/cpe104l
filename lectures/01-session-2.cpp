// * FUNCTIONS

// directives
#include <cmath> // math functions
#include <cctype> // character functions
#include <string> // string functions

#include <iostream>
using namespace std;

// main
int main() {
  /* // * Functions

    syntax:
      returnType functionName(parameters) {
        statements;
        return value;
      }

    types:
      1. predefined
          - library must be included in directives
      2. user-defined
          - value-returning
          - void

    parameters - placeholders for values to be passed to the function
      1. formal parameters - parameters in the function definition
      2. arguments/actual parameters - actual values passed to the function

    
    pass by:
      1. Value - pass a copy of the value
        - syntax: 
            returnType functionName(dataType parameterName) {
              statements;

              return value;
            }

      2. Reference - pass the address of the value
        - changes made to the value in the function will affect the original value
        - syntax: 
            returnType functionName(dataType& parameterName) {
              statements;

              return value;
            }
  */

  /* // * String
    Printing out of bounds: prints whitespace/blank)
    ! additional string operations
  
    str.erase(pos, n) - removes characters from a string then remaining characters are pushed to the left
    ! USE IN PROJECT: REPLACE DELETE FUNC
  */

 /* // * Array
    simples data structure

    syntax:
      dataType arrayName[arraySize];
  
 
 */

  return 0;
}