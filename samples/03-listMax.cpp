#include <iostream>
#include <cmath>
using namespace std;

// TODO

int main() {
  int size, max = -1;
  cout << "Enter the number of elements in the list: " << endl;
  cin >> size;

  int list[size];
  // get max num

  return 0;
}